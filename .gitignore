# General
# ------------------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.swp
*~
*.bak
*.tmp

# IDE / Editor specific
# ------------------------------------------------------------------------------
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (WebStorm, IntelliJ IDEA, etc.)
.idea/
*.iml
*.ipr
*.iws
# NetBeans
nbproject/
build/
nbbuild/
dist/
nbdist/
.nb-gradle/

# Node.js / npm / yarn (Common for both backend and frontend)
# ------------------------------------------------------------------------------
node_modules/
package-lock.json # If you use npm and commit package.json, sometimes this is committed too. Decide with your team.
yarn.lock         # If you use yarn and commit package.json, sometimes this is committed too. Decide with your team.
# If you choose NOT to commit lock files (generally not recommended for applications):
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Build outputs (Common)
# ------------------------------------------------------------------------------
dist/
build/
out/
coverage/
*.log
logs/
*.pid
*.seed
*.pid.lock

# Environment variables (CRITICAL - NEVER COMMIT THESE)
# ------------------------------------------------------------------------------
.env
.env.*
!.env.example # It's good practice to commit an example env file

# TypeScript specific (if not handled by outDir/dist)
# ------------------------------------------------------------------------------
*.tsbuildinfo

# Backend specific (Express.js / Node.js)
# ------------------------------------------------------------------------------
# Assuming your backend code is in a 'backend/' subdirectory
suprabhat-backend/node_modules/
suprabhat-backend/dist/
suprabhat-backend/coverage/
suprabhat-backend/.env
suprabhat-backend/.env.*
!suprabhat-backend/.env.example
suprabhat-backend/npm-debug.log*
suprabhat-backend/yarn-debug.log*
suprabhat-backend/yarn-error.log*
# Add any other backend-specific build outputs or logs

# Frontend specific (React.js - CRA / Vite)
# ------------------------------------------------------------------------------
# Assuming your frontend code is in a 'frontend/' subdirectory
suprabhat-web/node_modules/
suprabhat-web/build/      # For Create React App
suprabhat-web/dist/       # For Vite
suprabhat-web/coverage/
suprabhat-web/.env
suprabhat-web/.env.local
suprabhat-web/.env.development.local
suprabhat-web/.env.test.local
suprabhat-web/.env.production.local
!suprabhat-web/.env.example # If you have a separate example for suprabhat-web
suprabhat-web/npm-debug.log*
suprabhat-web/yarn-debug.log*
suprabhat-web/yarn-error.log*
# Create React App specific
suprabhat-web/build/**/*.map # Source maps if you don't want them in repo (usually large)
# Vite specific
suprabhat-web/dist/**/*.map # Source maps if you don't want them in repo

# Operating System specific files
# ------------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~

# Optional: Dependency directories if not using standard node_modules
# For example, if using pnpm's virtual store structure (though pnpm handles this well)
# .pnpm-store/

# Optional: Test reports if generated locally and not needed in repo
# test-results/
# .nyc_output/

# Docker
# ------------------------------------------------------------------------------
# .dockerignore # Use a separate .dockerignore file for Docker builds
# docker-compose.override.yml # If it contains sensitive local overrides